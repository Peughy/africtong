import 'package:flutter/material.dart';

class Lecons extends StatelessWidget {
  const Lecons({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Essai", style: TextStyle(fontSize: 48))
    );
  }
}